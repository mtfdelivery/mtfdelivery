[build]
  command = "flutter build web --release --no-tree-shake-icons"
  publish = "build/web"

[context.production.environment]
  FLUTTER_VERSION = "stable"
  GROQ_API_KEY = "placeholder" # Should be set in Netlify UI
